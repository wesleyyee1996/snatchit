import React from "react";
import { observer } from "mobx-react-lite";

const CannotMakeWordFromGameTilesAlert = observer(({ gameAlertStore }) => {
  return <div></div>;
});

export default CannotMakeWordFromGameTilesAlert;
