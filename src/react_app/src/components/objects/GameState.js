class GameState {
  constructor(gameStateJson) {
    const gameStateObj = JSON.parse(gameStateJson);
    // this.currPlayer = gameStateObj.currPlayer
    // this.players = gameStateObj.players
  }
}

export default GameState;
