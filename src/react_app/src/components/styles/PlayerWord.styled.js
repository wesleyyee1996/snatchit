import styled from "styled-components";

export const StyledPlayerWord = styled.div`
  display: inline-box;
  // flex-direction: row;
  text-align: left;
  max-height: 7vh;
  // max-width: ${(props) => props.width}vh;
  // justify-content: space-around;
`;
